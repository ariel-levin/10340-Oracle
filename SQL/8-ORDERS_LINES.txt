CREATE table "ORDERS_LINES" (
    "ORDER_NUM"        NUMBER NOT NULL,
    "LINE_NUM"         NUMBER NOT NULL,
    "ITEM_NUM"         NUMBER NOT NULL,
    "LINE_QUANTITY"    NUMBER NOT NULL,
    "LINE_PRICE"       FLOAT NOT NULL,
    "LINE_DISCOUNT"    NUMBER NOT NULL,
    "LINE_FINAL_PRICE" FLOAT NOT NULL
)
/

alter table "ORDERS_LINES" add constraint  "ORDERS_LINES_PK" primary key ("ORDER_NUM","LINE_NUM")
/

ALTER TABLE "ORDERS_LINES" ADD CONSTRAINT "ORDERS_LINES_ORDER_FK" 
FOREIGN KEY ("ORDER_NUM")
REFERENCES "ORDERS" ("ORDER_NUM")
ON DELETE CASCADE

/
ALTER TABLE "ORDERS_LINES" ADD CONSTRAINT "ORDERS_LINES_ITEM_FK" 
FOREIGN KEY ("ITEM_NUM")
REFERENCES "ITEMS" ("ITEM_NUM")

/
